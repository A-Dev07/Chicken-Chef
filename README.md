
  # Chicken Chef Food Ordering UI

  This is a code bundle for Chicken Chef Food Ordering UI. The original project is available at https://www.figma.com/design/elwV2khaS2uOG1w74IOrps/Chicken-Chef-Food-Ordering-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  